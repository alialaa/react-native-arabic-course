import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 20
    },
    card: {
        marginBottom: 10
    },
    label: {
        fontSize: 16
    },
    choices: {
        marginTop: 5
    }
});

export default styles;
