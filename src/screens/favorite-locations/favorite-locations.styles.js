import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    card: {
        marginHorizontal: 20,
        marginVertical: 7.5
    },
    locationName: {
        fontSize: 20
    }
});

export default styles;
